# -*- sh -*-

# Config file for arm build, linked against a non-root fs uclibc
# (i.e. for android).

UCLIBC=/data/uclibc

make clean
LDFLAGS=-Wl,--dynamic-linker=${UCLIBC}/lib/ld-uClibc.so.0 \
./configure --cc=arm-linux-uclibcgnueabi-gcc --prefix=${UCLIBC}/usr/ && make




