;; -*- scheme -*- 
;; Bootstrap using s-expression syntax.

( '((i       . (compile run))
    (compose . ((>lcode) dip >lcode lcompose))
    (pa      . ((abstract) dip >lcode lcompose))
    (see     . (find post))) define
 
 "libprim/PF" display cr
 repl)

